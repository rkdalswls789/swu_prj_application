spring.application.name=inclass-spring-security

server.port=9090

# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.database-platform=org.hibernate.dialect.MySQL8Dialect

# DataSource
spring.datasource.url=jdbc:mysql://database-1.cbi0gcuia4ey.ap-northeast-2.rds.amazonaws.com:3306/swu_prj_application?\
allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=applepi
spring.datasource.password=mysqlmysql
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JWT ??
jwt.issuer=devrary
jwt.secret_key=0aa067d8-9355-4c41-adb3-b18307078da4

########## dev only ########################
# Spring DevTools LiveReload
spring.devtools.livereload.enabled=true

# Thymeleaf cache disable
spring.thymeleaf.cache=false

# Logging level to debug
logging.level.org.springframework=DEBUG

# Spring Session JDBC Store
spring.session.store-type=jdbc
spring.session.jdbc.initialize-schema=always
############################################
