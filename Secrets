name: Environment Variable - Secrets

on: workflow_dispatch

jobs:
  secrets-test:
    runs-on: ubuntu-latest

    steps:
      - name: Access Secrets
        run: |
          echo "Secret Token : ${{secrets.username}}"
          echo "Secret Token : ${{secrets.password}}"
